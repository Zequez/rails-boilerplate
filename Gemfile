source 'https://rubygems.org'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.0.2'

# Use inherited resouces to tidy up controllers
gem 'inherited_resources'

# Use Devise to manage users and stuff
# gem 'devise'

# Use Active Admin for administration panels
#gem 'activeadmin', github: 'gregbell/active_admin'

# Use can can to manage devise permissions
#gem "cancan"

# Use sqlite3 as the database for Active Record
gem 'sqlite3'

# Use SCSS for stylesheets
gem 'sass-rails', '~> 4.0.0'

# Use foundation as CSS base
gem 'foundation-rails'

# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'

# Use CoffeeScript for .js.coffee assets and views
gem 'coffee-rails', '~> 4.0.0'

# Use HAML to make awesome views
gem 'haml'
gem 'haml-rails'

# Use Kaminari for pagination
# gem 'kaminari'

# Use angular JS for client side framework, disable for non-client-heavy apps
#gem 'angularjs-rails'

# See https://github.com/sstephenson/execjs#readme for more supported runtimes
# gem 'therubyracer', platforms: :ruby

# Use jquery as the JavaScript library
gem 'jquery-rails'

# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
gem 'turbolinks'

# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 1.2'

group :doc do
  # bundle exec rake doc:rails generates the API under doc/api.
  gem 'sdoc', require: false
end

# Behaviour driven testing
group :test, :development do
  gem 'rspec-rails' # For behaviour driven testing
  gem 'capybara' # For integration testing
  gem 'factory_girl' # For making factories
  gem 'shoulda' # For better testing
  gem 'guard' # For continuous texting
  gem 'guard-rspec', require: false
end

# Use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.1.2'

# Use unicorn as the app server
# gem 'unicorn'

# Use Capistrano for deployment
# gem 'capistrano', group: :development

# Use debugger
# gem 'debugger', group: [:development, :test]
